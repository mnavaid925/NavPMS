from django.apps import AppConfig


class SpendAnalyticsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.spend_analytics'
    label = 'spend_analytics'
    verbose_name = 'Spend Analytics & Reporting'
